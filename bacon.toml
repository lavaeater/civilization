#:schema https://dystroy.org/bacon/.bacon.schema.json

default_job = "clippy-all"
env.CARGO_TERM_COLOR = "always"

[jobs.check]
command = ["cargo", "+nightly", "check"]
need_stdout = false

[jobs.check.env]
RUSTC_WRAPPER = "sccache"
RUSTFLAGS = "-Z threads=8"

[jobs.clippy]
command = [
    "cargo", "+nightly", "clippy",
#    "-Z", "codegen-backend",
    "--timings"
]
need_stdout = true

[jobs.clippy.env]
#CARGO_PROFILE_DEV_CODEGEN_BACKEND = "cranelift"  # Dynamically assigns cranelift to dev builds
RUSTC_WRAPPER = "sccache"
RUSTFLAGS = "-Z threads=8"

# Same as `clippy`, but also lints tests, examples and benches — the default
# `clippy` job only sees the lib and bin targets.
[jobs.clippy-all]
command = [
    "cargo", "+nightly", "clippy",
#    "-Z", "codegen-backend",
    "--all-targets",
]
need_stdout = true

[jobs.clippy-all.env]
#CARGO_PROFILE_DEV_CODEGEN_BACKEND = "cranelift"
RUSTC_WRAPPER = "sccache"
RUSTFLAGS = "-Z threads=8"

[jobs.nextest]
command = [
    "cargo", "+nightly", "nextest", "run",
    "--timings"
]
need_stdout = true
analyzer = "nextest"

[jobs.nextest.env]
RUSTC_WRAPPER = "sccache"
RUSTFLAGS = "-Z threads=8"

[jobs.tarp]
 command = [
     "cargo", "+nightly", "tarpaulin", "--skip-clean"
]
need_stdout = true
analyzer = "nextest"

[jobs.tarp.env]
RUSTC_WRAPPER = "sccache"
RUSTFLAGS = "-Z threads=8"

[jobs.doc]
command = ["cargo", "doc", "--no-deps"]
need_stdout = false

# If the doc compiles, then it opens in your browser and bacon switches
# to the previous job
[jobs.doc-open]
command = ["cargo", "doc", "--no-deps", "--open"]
need_stdout = false
on_success = "back" # so that we don't open the browser at each change

# You can run your application and have the result displayed in bacon,
# if it makes sense for this crate.
[jobs.run]
command = [
    "cargo", "run",
    # put launch parameters for your program behind a `--` separator
]
need_stdout = true
allow_warnings = true
background = true

[keybindings]
# Remap 'c' or choose custom keys to trigger your profiling layouts on a whim
t = "job:tarp"
c = "job:clippy" # comment this to have 'c' run clippy on only the default target
a = "job:clippy-all" # clippy across lib, bins, tests and examples
n = "job:nextest"
